# .streamlit/secrets.toml
# This file is used for Streamlit Cloud deployment
# For local development, use .env file instead

# Add your database URL in Streamlit Cloud dashboard:
# Settings > Secrets > Add the following line:
# DATABASE_URL = "postgresql://username:password@host:port/database"

# DO NOT commit actual credentials to this file!
# This file serves as a template only.
